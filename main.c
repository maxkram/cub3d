#include "./cub3d.h"

// void ft_error(const char *message, int exit_status) {
//     printf("Error: %s\n", message);
//     exit(exit_status);
// }

int main(int ac, char **av)
{
	t_parse *parse;

	if (ac != 2)
	{
		printf("Error\n");
		return (0);
	}
}